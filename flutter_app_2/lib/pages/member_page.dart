import 'package:flutter/material.dart';

// 会员中心界面
class MemberPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('会员中心')));
  }
}
